B1 fixture for APO-633: one unit at a deep path, so its module name is long enough
to hit the module tag's `max-width: 30%` and ellipsise. Paired with a long resource
address so both compete for the header row.
